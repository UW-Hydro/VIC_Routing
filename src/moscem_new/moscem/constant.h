/* ======================================
some constants
Yuqiong Liu, March  2003
========================================*/

#ifndef _CONSTANT_H_
#define _CONSTANT_H_

#define EPS1 0.001
#define EPS2 1.0e-8
#define INF  1.0e+20
#define NINF -1.0e+20
#define PAR_NAME_LEN 10
#define PAR_LNAME_LEN 100
#define MAX_LINE_LEN  1000
#define MAX_FNAME_LEN  50
#define PI  3.14159265359
#ifndef MISSING_VALUE
#define MISSING_VALUE -999999.99999
#endif

#define max(m, n) ((m) > (n)  ? (m) : (n))
#define min(m, n) ((m) < (n)  ? (m) : (n))

#include <stdio.h>

#endif

